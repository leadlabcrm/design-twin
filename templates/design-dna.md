# Design DNA Worksheet — [site name]

> Fill every field with EXACT values copied from the file + source citation
> (class/variable/section). No approximations. Closed sets listed in full.

## 1. Typography
- Families & loading mechanism:
- @font-face sources / weights loaded:
- Font-size set (closed):
- Weight per role (h1/h2/h3/body/label/button):
- Line-height per tier:
- Letter-spacing per tier:
- Casing & punctuation habits:
- Wrapping habits (br in headings / p max-width / balance):

## 2. Color
- Backgrounds:
- Text tiers (primary/secondary/muted):
- Accent(s):
- Borders/dividers:
- Tokens/variables map (`--token-x` → value → role):
- Gradients (exact stops/angles):
- Color format convention (hex/rgb/rgba…):

## 3. Spacing & Layout
- Spacing set (closed):
- Section vertical rhythm (per section, desktop/mobile):
- Container max-width + side padding per breakpoint:
- Grid/flex habits (columns, gaps, alignment):
- Alignment rule (centered/left/mixed + the rule):

## 4. Shape & Surface
- Radius set (button/card/image/pill):
- Border recipes:
- Shadow strings per elevation:
- Blur/overlay/grain effects:

## 5. Imagery & Icons
- Icon system (set, stroke, sizes, inline?):
- Image treatment (radius/aspect/fit/overlays):

## 6. Component recipes
- Button variant 1 (full recipe incl. hover):
- Button variant 2 (if any):
- Link style:
- Card recipe:
- Badge/pill/input (if any):

## 7. Copy voice
- Heading style + examples from file:
- Body register:
- CTA verb pattern:
- Punctuation/casing fingerprints:

## 8. Motion sets (closed)
- Durations used:
- Easings/springs (exact strings/configs):
- Entrance pattern per section type (transform/opacity/delay/stagger):
- Hover transition recipes:
- Continuous animations:
- Animation mechanism in this file (CSS keyframes / appear-id JSON / observer script):

## 9. Breakpoints
- Breakpoint values:
- Mechanism (media queries / duplicated DOM variants / both):
- Per-breakpoint change patterns:

## 10. Plumbing
- Anchor/nav mechanism:
- Scripts present & their roles:
- Asset URL pattern:
- HTML comment usage (usually: none):
- Indentation/formatting style of the file:

## Quirks to preserve (02-D)
-
